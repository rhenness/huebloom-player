# Used by the local Vite server and local production builds.
# This is a client-side convenience gate, not secure authentication.
HUEBLOOM_LIBRARY_PASSWORD=change-me

# Required only for `npm run build`, not `npm run dev:ui`.
# HUEBLOOM_MEDIA_BASE_URL=https://media.githubusercontent.com/media/OWNER/REPOSITORY/main/
